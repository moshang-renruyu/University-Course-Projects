"""
函数的定义：
def函数名（传入参数）：    注：（）内部可以为空
     函数体
     return 返回值
函数的调用：
函数名(参数)
注意事项：
① 参数如不需要，可以省略（后续章节讲解）
② 返回值如不需要，可以省略（后续章节讲解）
③ 函数必须先定义后使用

"""
def say_hi():
    print("Hi,I am xuejingxuan")
say_hi()

